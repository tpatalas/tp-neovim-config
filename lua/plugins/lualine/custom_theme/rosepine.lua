return function(colors)
	return {
		normal = {
			a = { fg = colors.foam, bg = colors.surface },
			b = { fg = colors.muted, bg = colors.surface },
			c = { fg = colors.muted, bg = colors.surface },
			x = { fg = colors.muted, bg = colors.surface },
			y = { fg = colors.muted, bg = colors.surface },
			z = { fg = colors.pine, bg = colors.surface },
		},
		insert = {
			a = { fg = colors.rose, bg = colors.surface },
			b = { fg = colors.rose, bg = colors.surface },
			c = { fg = colors.rose, bg = colors.surface },
			x = { fg = colors.rose, bg = colors.surface },
			y = { fg = colors.rose, bg = colors.surface },
			z = { fg = colors.rose, bg = colors.surface },
		},
		visual = {
			a = { fg = colors.iris, bg = colors.surface },
			b = { fg = colors.iris, bg = colors.surface },
			c = { fg = colors.iris, bg = colors.surface },
			x = { fg = colors.iris, bg = colors.surface },
			y = { fg = colors.iris, bg = colors.surface },
			z = { fg = colors.iris, bg = colors.surface },
		},
		command = {
			a = { fg = colors.gold, bg = colors.surface },
			b = { fg = colors.gold, bg = colors.surface },
			c = { fg = colors.gold, bg = colors.surface },
			x = { fg = colors.gold, bg = colors.surface },
			y = { fg = colors.gold, bg = colors.surface },
			z = { fg = colors.gold, bg = colors.surface },
		},
		inactive = {
			a = { fg = colors.muted, bg = colors.surface },
			b = { fg = colors.muted, bg = colors.surface },
			c = { fg = colors.muted, bg = colors.surface },
			x = { fg = colors.muted, bg = colors.surface },
			y = { fg = colors.muted, bg = colors.surface },
			z = { fg = colors.muted, bg = colors.surface },
		},
	}
end
